---
export interface Props {
  class?: string;
}

const { class: externalClasses } = Astro.props;
---

<div
  class:list={[
    "bg-white rounded-md p-4 border border-slate-200",
    externalClasses,
  ]}
>
  <slot />
</div>
